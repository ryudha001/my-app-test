declare module 'react-native-loading-skeleton' {
  import { ComponentProps } from 'react';
  import { ViewStyle } from 'react-native';
  const Skeleton: React.FC<{ style?: ViewStyle }>;
  export default Skeleton;
}
